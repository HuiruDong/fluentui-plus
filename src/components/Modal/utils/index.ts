import { info, success, error, warning, confirm } from './staticModal';

export {
  // 静态方法
  info,
  success,
  error,
  warning,
  confirm,
};

// 导出常量配置
export { DEFAULT_TEXTS, DESTROY_DELAY } from './constants';
